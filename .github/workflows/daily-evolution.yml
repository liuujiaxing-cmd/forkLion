name: Daily Evolution

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
    inputs:
      use_ai:
        description: 'Use AI-powered evolution'
        required: false
        default: false
        type: boolean

permissions:
  contents: write
  pull-requests: write

jobs:
  evolve:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
      
      - name: Check if pet exists
        id: check_pet
        run: |
          if [ -f "lion_data/dna.json" ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi
      
      - name: Initialize pet (first time)
        if: steps.check_pet.outputs.exists == 'false'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          echo "🐾 First time setup - initializing pet..."
          python src/cli.py init --from-fork
      
      - name: Evolve pet (random evolution)
        if: steps.check_pet.outputs.exists == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          echo "🧬 Evolving pet (random evolution)..."
          python src/cli.py evolve --strength 0.2
      
      - name: Update README
        run: |
          echo "📝 Updating README..."
          python src/cli.py update-readme
      
      - name: Commit changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          git add lion_data/ README.md
          
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "🐾 Daily evolution: $(date +%Y-%m-%d)"
            git push
          fi
      
      - name: Show pet stats
        run: |
          echo "📊 Current pet stats:"
          python src/cli.py show
